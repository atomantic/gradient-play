# Chrome DevTools Protocol endpoint (PortOS default)
CDP_ENDPOINT=http://127.0.0.1:5556

# Game base URL
GAME_URL=https://game.gradient-bang.com

# Server port. Avoid PortOS core range 5554-5561, and ports taken by common
# PortOS apps on this user's fleet: 5563 (Critical Mass), 5570 (pixie-forge /
# critical-mass engine), 5813 (wagbook), 5161 (void-mud), 5551 (EscapeMint),
# 5553 (FableLoom). 5572 is clear.
PORT=5572
HOST=127.0.0.1
